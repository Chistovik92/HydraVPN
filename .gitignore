*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties

# Бинарник wrapper'а не коммитим — CI генерирует его сам (см. docs/HANDOFF.md)
gradle/wrapper/gradle-wrapper.jar

# Не коммитим тяжёлые нативные ядра — собираются отдельно (см. docs/BUILD.md)
/app/libs/*.aar
/app/libs/*.so
/app/src/main/jniLibs/**/*.so

# Ключи подписи
*.jks
*.keystore
keystore.properties

# Артефакты релиза (собираются scripts/release.sh, живут только на GitHub Releases)
/Hydra-*.apk

# AmneziaWG: libwg-go.so по ABI (scripts/build-awg.sh)
/app/libs/awg/
# olcRTC: libolcrtc.so по ABI (scripts/build-olcrtc.sh)
/app/libs/olcrtc/
